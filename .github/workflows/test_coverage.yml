name: Golden (uv.lock) tests on all OSes

on:
  push:
  pull_request:

permissions:
  contents: write
  checks: write
  pull-requests: write

jobs:
  golden:
    name: Golden env (uv.lock) on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up CPython
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.14"

      - name: Install uv
        uses: astral-sh/setup-uv@cec208311dfd045dd5311c1add060b2062131d57 # v8.0.0
        with:
          enable-cache: true
            

      # Verify uv.lock matches pyproject.toml (+ test extra)
      - name: Check lockfile consistency
        run: uv lock --check

      # Non-negotiable: exact env from uv.lock (+ test deps)
      - name: Sync (locked)
        run: uv sync --locked --extra test

      - name: Build coverage file
        shell: bash
        run: |
          uv run pytest --junitxml=pytest.xml \
            --cov=pyPLUTO --cov-report=term-missing Tests/ \
            | tee pytest-coverage.txt

      # Post coverage comment only once (avoid 3 PR comments)
      - name: Pytest coverage comment
        if: matrix.os == 'ubuntu-latest'
        id: coverageComment
        uses: MishaKav/pytest-coverage-comment@main
        with:
          pytest-coverage-path: ./pytest-coverage.txt
          junitxml-path: ./pytest.xml

      - name: Create the Badge
        if: matrix.os == 'ubuntu-latest'
        uses: schneegans/dynamic-badges-action@0e50b8bad39e7e1afd3e4e9c2b7dd145fad07501 # v1.8.0
        with:
          auth: ${{ secrets.PYTEST_COVERAGE_COMMENT }}
          gistID: 384b1f3a3a3b74cdbd65c4e3dce0632f
          filename: pytest-coverage-comment__main.json
          label: Coverage
          message: ${{ steps.coverageComment.outputs.coverage }}
          color: ${{ steps.coverageComment.outputs.color }}
          namedLogo: pytest
